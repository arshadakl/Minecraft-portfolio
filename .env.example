# Contact form — forwards to the main portfolio's contact API.
# Copy this file to .env.local and adjust if needed.

# The upstream endpoint that actually sends the mail.
CONTACT_UPSTREAM_URL=https://www.example.in/api/contact

# Source tag added to the forwarded subject/message so replies are traceable.
CONTACT_SOURCE=minecraft.arshadakl.in
